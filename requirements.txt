# Required dependencies
python>=3.12
numpy
scipy
matplotlib
dill
pandas
tqdm
pytest
# Optional: for better performance on Apple Silicon Macs
# Install separately with conda: conda install "libblas=*=*accelerate"