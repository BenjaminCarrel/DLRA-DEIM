# Experiments

This folder contains experiment scripts that can be run after installing the package.

## Usage

After installing the package with `pip install -e .`, you can run any experiment script:

```bash
python experiments/example_experiment.py
```
